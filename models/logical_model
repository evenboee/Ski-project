// Ski:
Ski_model(model, skiing_type, description, grip_system, historical, temperature, url)
PK model

Ski_type(size, weight_class, MSRP, model)
PK size, weight_class
FK model REFERENCES Ski_model(model)

Ski(product_number, production_date, model, size, weight)
PK product_number
FK model REFERENCES Ski_model(model)
FK size, weight REFERENCES Ski_type(size, weight)

// Customer
Team_skier(id, start_date, end_date, name, dob, club, num_skies)
PK id

Corporation(id, start_date, end_date, name, shipping_address, buying_price)
PK id
AK name (alternate key)

Franchise(franchise_id, store_id)
PK franchise_id, store_id
FK franchise_id REFERENCES Corporation(id)
FK store_id REFERENCES Corporation(id)

//Order:
Order(order_number, total_price, state, ref_larger_order, customer_id, shipment_number)
PK order_number
FK customer_id REFERENCES Corporation(id)
FK shipment_number REFERENCES Shipment(number)

SkiType_order(order_number, size, weight, quantity)
PK order_number, size, weight
FK order_number REFERENCES Order(order_number)
FK size REFERENCES Ski_type(size)
FK weight REFERENCES Ski_type(weight_class)

// Transport
Transporter(company_name)
PK company_name

Shipment(number, store_name, shipping_address, pickup_date, state, driver_id, repNo)
PK number
FK repNo REFERENCES Customer_representative(number)

// Employees:
// Department being 'customer rep', 'storekeeper' or 'production planner'
Employee(number, name, department)
PK number

Order_log(employee_number, order_number old_state, new_state, time)
PK employee_number, order_number
FK employee_number REFERENCES Employee(number)
FK order_number REFERENCES Order(order_number)

//Production:
Production_plan(id, start_date, end_date, plannerNo)
PK id
FK plannerNo REFERENCES Employee(number)

Production_plan_reference(plan_id, size, weight, quantity)
PK plan_id, size, weight
FK size REFERENCES Ski_type(size)
FK weight REFERENCES Ski_type(weight_class)
